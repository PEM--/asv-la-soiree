@import 'nib'
@import 'jeet'
@import 'typographic'
@import 'client/views/admin/*.import'

@import s(basePath, animate)

.admin
  >h1
    display none
  .login
    center(960px)
    width 60%
    min-width 300px
    display flex
    flex-direction column
    justify-content center
    align-items center
    align-content center
    .at-form
      animation fadeInDown longAnimationValue
      width 100%

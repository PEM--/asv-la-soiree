@import 'nib'
@import 'jeet'
@import 'client/styles/*.import'

.main-container
  footer
    transform translateZ(0)
    width 100%
    margin 0
    padding 0
    z-index 1
    background bgColor
    color textColor
    >section
      display inline-block
      center(maxSize)

@import 'nib'
@import 'jeet'
@import 'client/styles/*.import'

.main-container
  transition opacity defaultTransitionValue
  margin 0
  padding 0
  size 100%
  >nav
    opacity .5
    fixed top left
    background textColor
    //background yellow
    z-index 1000
    width 100%
    display flex
    justify-content center
    >.nav
      width 100%
      max-width maxSize
      display flex
      >.logo
        display inline-block
        relative top .7em left
        size 11em 4em
        //background red
        >.svg-logo-container
          display inline-block
          relative top left
          asvlasoireeBigLogo(100%)
          vertical-align middle
          cursor pointer
      >ul
        flex-grow 1
        //background violet
        display flex
        flex-wrap no-wrap
        align-content flex-end
        justify-content flex-end
        inline-list()
        >li
          margin 1em
          font-size 1.3em
          font-family BaronNeue
          >a
            transition color .3s ease-in-out
            color bgColor
        >li.active
          >a
            color brandColor

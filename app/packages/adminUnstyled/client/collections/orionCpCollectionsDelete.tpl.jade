// orionCpCollectionsDelete
+Layout template='orionCpTitle'
  | Delete {{ collection.singularName }}
+Layout template='orionCpContentContainer'
  p Are you sure you want to delete this {{ collection.singularName }}?
  if item.canShowRemove
    a.user-btn-action(href="{{ collection.indexPath }}") Cancel
    a.user-btn-action-warning.confirm-delete Confirm

// orionCpAccountsInvite
+Layout template='orionCpTitle'
  | Invite
+Layout template='orionCpContentContainer'
  if invitationId
    .alert.alert-success
      | Invitation created successfully
    p
      | Link for the invitation page:
      a(href="{{ pathFor 'registerWithInvitation' _id=invitationId }}") {{ urlFor 'registerWithInvitation' _id=invitationId }}
    a.user-btn-action(href="{{ pathFor 'accounts.index' }}") Back
    button.btn-invite-another Invite another
  else
    form.invite
      p Select the roles for the new user
      each roles
        label
          input(type="checkbox", role="{{ this }}")
          span.role(data-attr=this)= this
      p Email of the new user (optional)
      input(type='email')
      a.user-btn-action(href="{{ pathFor 'accounts.index' }}") Cancel
      button Invite
